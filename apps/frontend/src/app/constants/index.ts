import { Status } from '../types/types';

export const initialTask = {
  title: '',
  description: '',
  status: Status.OPEN,
};

export const intialToast = {
  message: '',
  type: '',
  show: false,
};
